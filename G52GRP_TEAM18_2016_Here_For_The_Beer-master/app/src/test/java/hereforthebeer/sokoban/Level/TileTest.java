package hereforthebeer.sokoban.Level;

import org.junit.Test;

import static org.junit.Assert.*;

public class TileTest {/*
    @Test
    public void isCollidable() throws Exception {
        Tile t1 = new Tile(true, false);
        assertTrue(t1.isCollidable());

        Tile t2 = new Tile(false, false);
        assertFalse(t2.isCollidable());
    }

    @Test
    public void isGoal() throws Exception {
        Tile t1 = new Tile(true, false);
        assertFalse(t1.isGoal());

        Tile t2 = new Tile(true, true);
        assertFalse(t2.isGoal());

        Tile t3 = new Tile(false, true);
        assertTrue(t3.isGoal());
    }*/
}